{
  "name": "midi-sf2-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Web MIDI API override backed by a Tampermonkey SF2 resource and Web Audio.",
  "scripts": {
    "build": "node scripts/clean-dist.mjs && rollup -c",
    "serve": "node scripts/serve-demo.mjs",
    "test": "node scripts/smoke-test.mjs"
  },
  "devDependencies": {
    "rollup": "^4.41.1"
  }
}
