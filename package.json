{
  "name": "midi-sf2-web",
  "version": "0.1.3",
  "private": true,
  "type": "module",
  "description": "Web MIDI API override backed by a cached SF2 SoundFont and Web Audio.",
  "scripts": {
    "build": "node scripts/clean-dist.mjs && rollup -c",
    "serve": "node scripts/serve-demo.mjs",
    "test": "node scripts/smoke-test.mjs",
    "version:bump-userscript": "node scripts/bump-userscript-version.mjs"
  },
  "devDependencies": {
    "rollup": "^4.41.1"
  }
}
